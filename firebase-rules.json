{
  "//": "Firebase Realtime Database güvenlik kuralları — Firebase konsolunda",
  "//1": "Realtime Database → Rules bölümüne yapıştırılıp Publish edilmeli.",
  "//2": "ÖNEMLİ: Bu dosyayı repoya koymak yetmez; kurallar yalnız konsolda",
  "//3": "yayınlandıklarında geçerli olur. Yayınlanmazsa veritabanı adresi",
  "//4": "bilinen herkes bütün kayıtları okuyup silebilir.",
  "//5": "Varsayılan 'test modu' kuralları (.read/.write true) mutlaka",
  "//6": "değiştirilmeli — süresi dolduğunda uygulama da çalışmaz.",

  "rules": {
    ".read": false,
    ".write": false,

    "kayitlar": {
      "//": "Giriş yapmış (anonim de olsa) istemciler okuyabilir/yazabilir.",
      "//2": "Silme kapalı: bir kaydı yok etmek yerine üzerine yazılır.",
      ".read": "auth != null",
      "$kayitId": {
        ".write": "auth != null && newData.exists()",
        ".validate": "newData.hasChildren(['id','type','createdAt'])",
        "id":        { ".validate": "newData.isNumber()" },
        "type":      { ".validate": "newData.isString() && newData.val().length <= 16" },
        "text":      { ".validate": "newData.isString() && newData.val().length <= 5000" },
        "createdAt": { ".validate": "newData.isString() && newData.val().length <= 40" },
        "mudur":     { ".validate": "newData.isString() && newData.val().length <= 80" },
        "mudurId":   { ".validate": "newData.isString() && newData.val().length <= 40" },
        "items":     { ".validate": "newData.hasChildren() || newData.val() == null" },
        "$diger":    { ".validate": true }
      }
    }
  }
}
